import React from "react"
import studioAlbums from "../db/studio"

const StudioQuiz = (props) => {
    
    console.log(studioAlbums)

    return (
        <div>Studio Albums Quiz</div>
    )
}

export default StudioQuiz